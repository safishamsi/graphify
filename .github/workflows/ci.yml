name: CI

on:
  push:
    branches: ["v1", "v2", "v3", "v4", "v5", "v6", "v7", "v8", "main"]
  pull_request:
    branches: ["v1", "v2", "v3", "v4", "v5", "v6", "v7", "v8", "main"]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[mcp,pdf,watch,sql]"
          # tree-sitter-dm 0.25.0's PyPI artifact is broken (sdist missing
          # scanner.c, no wheels). Install from git until 0.25.1 ships, then
          # this line can be replaced with adding `dreammaker` to the extras.
          pip install "tree-sitter-dm @ git+https://github.com/FeudeyTF/tree-sitter-dm.git"
          pip install pytest

      - name: Run tests
        run: |
          python -m pytest tests/ -q --tb=short

      - name: Verify install works end-to-end
        run: |
          graphify --help
          graphify install
