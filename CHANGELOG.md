# Changelog

All notable changes to the **depOS** documentation and application workstream will be documented here.

Historical **graphify** release notes predating the depOS documentation layout were removed when docs were consolidated. Refer to git history for prior graphify changelog entries.

## [Unreleased]

- **depOS:** Python package `depos/` — snapshot via `graphify.extract` + `build_from_json`, SARIF fusion, blast radius, federation, CODEOWNERS hints, FastAPI (`depos-api`), post-CI correlation, LLM export and `llm_tools`.
- **Apps:** `apps/web` (Next.js shell), `apps/worker` (README), `.github/workflows/depos-ci.yml`.
- Documentation: root README replaced with depOS; added `docs/` (product, architecture, graphify internals, development).
- Removed per-locale graphify README copies (ja, zh-CN, ko-KR) in favor of English depOS docs.
- Replaced root `ARCHITECTURE.md` with `docs/architecture.md` and `docs/graphify-internals.md`.
