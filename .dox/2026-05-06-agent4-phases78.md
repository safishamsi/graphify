# Phase 7-8 Live Code Verification — Cache Safety & CLI Integration

> Date: 2026-05-06  
> Source runbook: `.dox/2026-05-05-deterministic-semantic-extraction-final-implementation-runbook.md`  
> Verdict: **PASS — both phases accurately describe live code. Minor line drift noted. No blocking issues.**

---

## Phase 7 — Cache Safety

### 7.1 Function Signature Verification

| Runbook claim | Runbook line | Live code line | Signature match? |
|---|---|---|---|
| `_GRAPHIFY_OUT` constant | L13 | `cache.py:13` | **EXACT** |
| `load_cached(path, root, kind)` | L77 | `cache.py:77` | **EXACT** |
| `save_cached(path, result, root, kind)` | L108 | `cache.py:108` | **EXACT** |
| `cached_files(root)` | L148 | `cache.py:148` | **EXACT** |
| `clear_cache(root)` | L163 | `cache.py:163` | **EXACT** |

All five anchor points match exactly. No signature drift.

### 7.2 Cache Namespace Structure

**Runbook claim (lines 2746-2749):** `cached_files()` and `clear_cache()` hardcode only `("ast", "semantic")` while `cache_dir()`, `load_cached()`, and `save_cached()` accept arbitrary `kind` values.

**Live code confirmation:**

- `cache.py:64-74` (`cache_dir`): Accepts any `kind: str` parameter. Creates `graphify-out/cache/{kind}/` dynamically. **CONFIRMED.**
- `cache.py:77-105` (`load_cached`): Accepts any `kind: str` parameter, defaults to `"ast"`. **CONFIRMED.**
- `cache.py:108-145` (`save_cached`): Accepts any `kind: str` parameter, defaults to `"ast"`. **CONFIRMED.**
- `cache.py:156`: Hardcoded `for kind in ("ast", "semantic"):` — only two namespaces. **CONFIRMED.**
- `cache.py:171`: Hardcoded `for kind in ("ast", "semantic"):` — only two namespaces. **CONFIRMED.**
- No `_KNOWN_CACHE_KINDS` constant exists in the live code. **CONFIRMED — fix not yet applied.**

**Assessment:** The runbook's diagnosis is accurate. The low-level cache primitives (`cache_dir`, `load_cached`, `save_cached`) are already namespace-agnostic. The two convenience functions (`cached_files`, `clear_cache`) have a maintainability gap: they hardcode `("ast", "semantic")` and would silently skip any future `"deterministic"` namespace. The proposed fix (adding `_KNOWN_CACHE_KINDS = ("ast", "semantic", "deterministic")` and iterating over it) is the correct minimal change and requires no API breakage.

### 7.3 Phase 7 Verdict

**SAFE TO IMPLEMENT.** The runbook's proposed changes to `cached_files()` and `clear_cache()` are purely additive — they extend existing behavior without changing signatures or removing functionality. No existing tests should break. The two new tests proposed at runbook lines 2816-2835 exercise the deterministic namespace path and are compatible with the existing test infrastructure at `tests/test_cache.py:174-216`.

---

## Phase 8 — CLI Integration Policy

### 8.1 Insertion Point Verification

| Runbook claim | Runbook line | Live code line | Match? |
|---|---|---|---|
| AST extraction on code files | L2144-2160 | `__main__.py:2144-2154` | **Close** (6-line drift, same structure) |
| LLM semantic extraction on docs/papers/images | L2162-2210 | `__main__.py:2156-2211` | **Close** (6-line drift, same structure) |
| Merge AST + semantic | L2215-2223 | `__main__.py:2213-2223` | **Close** (2-line drift, same structure) |
| Build graph | L2267-2294 | `__main__.py:2267-2294` | **EXACT** |

The line drift (2-6 lines) is consistent across all three anchor points and is caused by additional inline import statements in the semantic extraction block (`cache.py:2157-2160` import `check_semantic_cache` and `save_semantic_cache` inline). The structural flow is identical.

### 8.2 New CLI Flags Since Runbook Was Written

The `graphify extract` command has these flags in live code not explicitly listed in the runbook's help output:

| Flag | Live line | Conflicts with runbook? |
|---|---|---|
| `--model M` | `__main__.py:2035-2038` | No — model override for LLM backend |
| `--dedup-llm` | `__main__.py:2045-2046` | No — LLM-assisted dedup toggle |
| `--global` | `__main__.py:2047-2048` | No — merges into global graph |
| `--as <tag>` | `__main__.py:2049-2050` | No — repo tag for `--global` |

**None of these conflict** with the runbook's Phase 8 direction because:

1. The runbook explicitly declares "Do not change `graphify/__main__.py`" — it's a deliberate non-change.
2. The new flags control LLM backend selection and graph output routing, not extraction behavior.
3. The deterministic extraction improvements happen entirely inside `graphify.extract.extract()`, which is called at `__main__.py:2151` regardless of flags.

### 8.3 Merge Step Verification

**Runbook claim (lines 2861-2863):** The merge is a simple list concatenation of AST + semantic nodes/edges.

**Live code at `__main__.py:2217-2223`:**

```python
merged: dict = {
    "nodes": list(ast_result.get("nodes", [])) + list(sem_result.get("nodes", [])),
    "edges": list(ast_result.get("edges", [])) + list(sem_result.get("edges", [])),
    "hyperedges": list(sem_result.get("hyperedges", [])),
    "input_tokens": ast_result.get("input_tokens", 0) + sem_result.get("input_tokens", 0),
    "output_tokens": ast_result.get("output_tokens", 0) + sem_result.get("output_tokens", 0),
}
```

**CONFIRMED.** The merge is indeed a simple list concatenation. The runbook's assumption that "new deterministic extraction helpers are internal AST-extraction improvements" that "the existing `graphify extract` command automatically receives them because it calls `graphify.extract.extract()` for code files" is correct. The new deterministic nodes/edges produced inside `extract()` would flow through `ast_result` and be merged into the final graph without any CLI changes.

### 8.4 Phase 8 Verdict

**SAFE TO SKIP.** The runbook's Phase 8 policy ("make no CLI changes") is correct and well-reasoned. The deterministic extraction is an internal AST improvement that requires zero CLI surface changes. Adding CLI flags at this stage would be premature.

---

## Cross-Phase Summary

| Check | Phase | Status |
|---|---|---|
| `_GRAPHIFY_OUT` constant still at L13 | 7 | PASS |
| `load_cached()` signature at L77 | 7 | PASS |
| `save_cached()` signature at L108 | 7 | PASS |
| `cached_files()` signature at L148 | 7 | PASS |
| `clear_cache()` signature at L163 | 7 | PASS |
| Hardcoded `("ast", "semantic")` in `cached_files()` | 7 | CONFIRMED (fix needed) |
| Hardcoded `("ast", "semantic")` in `clear_cache()` | 7 | CONFIRMED (fix needed) |
| `cache_dir()` accepts arbitrary `kind` | 7 | CONFIRMED |
| AST extraction insertion point | 8 | PASS (6-line drift, same structure) |
| Semantic extraction insertion point | 8 | PASS (6-line drift, same structure) |
| Merge insertion point | 8 | PASS (2-line drift, same structure) |
| Build graph insertion point | 8 | PASS (exact match) |
| New CLI flags conflict with deterministic extraction | 8 | NO CONFLICT |
| Merge step is simple list concatenation | 8 | CONFIRMED |

**Overall: Both phases are safe to implement/follow as written. No live-code divergence that would block execution.**
